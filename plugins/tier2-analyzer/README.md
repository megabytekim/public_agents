# Tier 2 Analyzer Plugin

> stock-analyzer-advanced의 Tier 2 심층 분석 확장

## 개요

Tier 1 (`stock-analyzer-advanced`)의 1차 스크리닝 후, **심층 펀더멘털 분석**을 수행하는 에이전트 시스템.

## 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         TIER 1 (스크리닝)                        │
│                    plugins/stock-analyzer-advanced               │
├─────────────────────────────────────────────────────────────────┤
│    ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐        │
│    │   MI   │    │   SI   │    │   TI   │    │   FI   │        │
│    │ (뉴스) │    │(종토방)│    │(기술적)│    │(연간)  │        │
│    └────────┘    └────────┘    └────────┘    └────────┘        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Tier 1 분석 완료 후
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         TIER 2 (심층분석)                        │
│                    plugins/tier2-analyzer                        │
├─────────────────────────────────────────────────────────────────┤
│    ┌──────────────┐   ┌──────────────┐   ┌──────────────┐      │
│    │     FI+      │   │     SI+      │   │     MI+      │      │
│    ├──────────────┤   ├──────────────┤   ├──────────────┤      │
│    │ ✅ 구현완료   │   │ 🔜 예정      │   │ 🔜 예정      │      │
│    │ 분기 재무    │   │ 텔레그램     │   │ 경영진 평가  │      │
│    │ + 피어비교   │   │ 센티먼트     │   │ + DART 공시  │      │
│    └──────────────┘   └──────────────┘   └──────────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

## 프로젝트 구조

```
tier2-analyzer/
├── README.md
├── agents/
│   └── fi-plus.md          # FI+ 에이전트 정의
├── utils/
│   ├── __init__.py         # 패키지 export
│   └── fi_plus/            # FI+ 모듈
│       ├── __init__.py
│       ├── peer_comparison.py
│       └── fnguide/
│           ├── __init__.py # 통합 함수
│           └── parser.py   # 파싱 로직
├── tests/
│   ├── test_fi_plus.py
│   ├── test_peer_comparison.py
│   └── test_quarterly_scraper.py
└── docs/
    ├── fnguide-quarterly-structure.md
    ├── logs/
    └── plans/
```

## FI+ 사용법

### 기본 사용

```python
from utils.fi_plus import get_full_financials, get_peer_comparison

# 전체 재무제표 (단일 HTTP 요청)
data = get_full_financials("005930")  # 삼성전자

print(data["income_statement"]["annual"])   # 연간 손익계산서
print(data["income_statement"]["quarterly"]) # 분기 손익계산서
print(data["balance_sheet"]["annual"])      # 연간 재무상태표
print(data["cash_flow"]["annual"])          # 연간 현금흐름표
print(data["ratios"])                       # 재무비율 (ROE, ROA, 부채비율 등)
print(data["growth"])                       # 성장률 (매출 YoY, 영업이익 YoY)

# 피어 비교
peer_data = get_peer_comparison("377300", ["035420", "035720"])
print(peer_data["sector_avg"])              # 섹터 평균 PER/PBR
print(peer_data["premium_discount"])        # 프리미엄/디스카운트 (%)
```

### 개별 함수

```python
from utils.fi_plus import (
    get_annual_income,        # 연간 손익계산서
    get_quarterly_income,     # 분기 손익계산서
    get_annual_balance_sheet, # 연간 재무상태표
    get_annual_cash_flow,     # 연간 현금흐름표
    get_sector_average,       # 섹터 평균 계산
)
```

### 하위 호환성

```python
# 기존 API 호환
from utils import (
    get_fnguide_quarterly,
    get_fnguide_annual_income,
    get_fnguide_annual_balance_sheet,
    get_fnguide_annual_cash_flow,
    get_fnguide_full_financials,
)
```

## 데이터 소스

| 데이터 | 소스 | 테이블 ID |
|--------|------|-----------|
| 손익계산서 (연간) | FnGuide | divSonikY |
| 손익계산서 (분기) | FnGuide | divSonikQ |
| 재무상태표 (연간) | FnGuide | divDaechaY |
| 현금흐름표 (연간) | FnGuide | divCashY |
| 밸류에이션 | Naver Finance | - |

## 재무비율 계산

| 비율 | 공식 |
|------|------|
| 부채비율 | 부채 / 자본 × 100 |
| 유동비율 | 유동자산 / 유동부채 × 100 |
| ROE | 순이익 / 자본 × 100 |
| ROA | 순이익 / 자산 × 100 |
| FCF | 영업CF + 투자CF |

## 테스트

```bash
cd plugins/tier2-analyzer
python -m pytest tests/ -v
```

## 에이전트

| 에이전트 | 상태 | 역할 | 소스 |
|----------|------|------|------|
| **FI+** | ✅ 완료 | 분기 재무 + 피어 비교 | FnGuide |
| **SI+** | 🔜 예정 | 텔레그램 센티먼트 | Telethon |
| **MI+** | 🔜 예정 | 경영진 평가 + DART 공시 | OpenDART API |

## 출력 경로

```
tier2/
├── 케이옥션_102370/
│   └── FI_PLUS_REPORT.md
├── 카카오페이_377300/
│   └── FI_PLUS_REPORT.md
└── ...
```

## 의존성

- Tier 1: `stock-analyzer-advanced` 플러그인
- Python: requests, beautifulsoup4
- (예정) telethon, dart-fss

## 관련 문서

- [FnGuide 테이블 구조](./docs/fnguide-quarterly-structure.md)
- [FI+ 구현 로그](./docs/logs/2026-01-17-fi-plus-implementation.md)
